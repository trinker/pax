% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ann.R
\name{ann}
\alias{ann}
\alias{roxann}
\title{Add Leading Symbol Annotations}
\usage{
ann(object = "clipboard", symbol = "##", space = 1, copy2clip = TRUE,
  ...)

roxann(object = "clipboard", symbol = "#'", space = 1, copy2clip = TRUE,
  ...)
}
\arguments{
\item{object}{An object to add commenting to.  If \code{"clipboard"} copies 
from the clipboard.}

\item{symbol}{The symbol(s) to use as the leading characters.}

\item{space}{How many spaces to put between the symbol and the object lines.}

\item{copy2clip}{logical,  If \code{TRUE} the output is copied to the clipboard.}

\item{\ldots}{ignored.}
}
\description{
\code{ann} - Add leading symbol annotation (e.g., comment characters).

\code{roxann} - Add leading roxygen symbol annotation (i.e., \code{#' }).
}
\examples{
\dontrun{
ann(mtcars)
roxann(mtcars)
ann(mtcars, symbol = "\%\%")  # LaTeX

library(clipr)
clipr::write_clip(capture.output(new_r))
ann()
}
}
